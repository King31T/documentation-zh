# getAccountBalance

**支持的 API**：`wallet`

TRON API 方法，检索特定账户的 TRX 余额及相关余额信息。该方法提供全面的余额数据，包括可用余额、冻结余额和委托资源。

```protobuf
rpc GetAccountBalance (AccountBalanceRequest) returns (AccountBalanceResponse) {}
```
