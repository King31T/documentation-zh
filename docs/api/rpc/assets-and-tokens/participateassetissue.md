# participateAssetIssue

**支持的 API**：`wallet`

TRON API 方法，创建一个用于参与 TRC10 代币首次代币发行（ICO）的未签名交易。允许用户在发行期间购买新发行的 TRC10 代币。

```protobuf
rpc ParticipateAssetIssue (ParticipateAssetIssueContract) returns (Transaction) {}
```
