# createAssetIssue

**支持的 API**：`wallet`

TRON API 方法，创建一个用于在 TRON 网络上发行新 TRC10 代币的未签名交易。TRC10 代币是 TRON 的原生资产，无需部署智能合约即可创建。

```protobuf
rpc CreateAssetIssue (AssetIssueContract) returns (Transaction) {}
```
