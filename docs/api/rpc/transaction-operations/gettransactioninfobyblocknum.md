# getTransactionInfoByBlockNum

**支持的 API**：`wallet` `walletsolidity`

TRON API 方法，检索特定区块编号内所有交易的详细交易信息。

```protobuf
rpc GetTransactionInfoByBlockNum (NumberMessage) returns (TransactionInfoList) {}
```
