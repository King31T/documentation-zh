# getDelegatedResourceV2

**支持的 API**：`wallet` `walletsolidity`

TRON API 方法，使用 Stake 2.0 机制检索 TRON 区块链上账户之间的委托资源信息。

```protobuf
rpc GetDelegatedResourceV2 (DelegatedResourceMessage) returns (DelegatedResourceList) {}
```
