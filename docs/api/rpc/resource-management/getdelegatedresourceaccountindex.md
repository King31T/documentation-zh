# getDelegatedResourceAccountIndex

**支持的 API**：`wallet` `walletsolidity`

TRON API 方法，检索账户的委托索引，显示向指定账户委托资源或从指定账户接收资源的所有账户。

```protobuf
rpc GetDelegatedResourceAccountIndex (BytesMessage) returns (DelegatedResourceAccountIndex) {}
```
