# getCanDelegatedMaxSize

**支持的 API**：`wallet` `walletsolidity`

TRON API 方法，检索账户可以委托的带宽和能量资源的最大数量。

```protobuf
rpc GetCanDelegatedMaxSize (CanDelegatedMaxSizeRequestMessage) returns (CanDelegatedMaxSizeResponseMessage) {}
```
